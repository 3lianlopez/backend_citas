spring.application.name=citas


#prod
server.port=${PORT:8080}
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

#dev
#
#spring.datasource.url=jdbc:postgresql://localhost:5455/cita_db
#spring.datasource.username=postgres
#spring.datasource.password=Kinect1593
#
#spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.show-sql=true

# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${GMAIL_USERNAME}
spring.mail.password=${GMAIL_APP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.test-connection=false
app.mail.from=${GMAIL_USERNAME}

# JWT: secreto Base64 de al menos 256 bits y duración de 1 hora.
app.jwt.secret=${JWT_SECRET}
app.jwt.expiration-ms=3600000